<% provide(:title, 'All users') %>
<h1>Participants:</h1>

<h3 style="float: right">Total participants:  <%= @users.count %></h3>
<%= will_paginate %>

<ul class="users",>
  <%= render @users %>
</ul>

<%= will_paginate %>