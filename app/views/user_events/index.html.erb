<% for user_event in UserEvent.all %>
  id: <%= id = user_event.id %>
  user_id <%= user_event.user_id %>
  event_id <%= user_event.event_id %>
  <%= link_to "Delete", user_event_path(id), method: :delete,
              data:{confirm: "Are you sure?"} %>
  <br/>
<% end %>